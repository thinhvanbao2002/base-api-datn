export enum OrderType {
	PENDING = "pending",
	CONFIRMED = "confirmed",
	PROCESSING = "progress",
	SHIPED = "shiped",
	COMPLETED = "completed",
	CANCELLED = "cancelled",
}

export enum PayTypes {
	PAID = "pay",
	NOT_PAID = "notpay",
}
